export interface createClassification {
    name: String,
    identification: String,
    refs: String
}

export interface getOneClassification {
    name: String,
    identification: String,
    refs: String
}

export interface modifyClassification {
    name: String,
    identification: String,
    refs: String
}